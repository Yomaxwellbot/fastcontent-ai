import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "FastContent AI — Repurpose Content in Seconds",
  description:
    "Paste any blog post, transcript, or article and get Twitter threads, LinkedIn posts, and newsletter drafts instantly. Built by Maxwell.",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
